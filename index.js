//message to user of secret combination
alert(`You have received this message because you have been chosen to open an important vault. Here is the secret combination:`);
//use add, subtract, multiply 
const codeAdd = (5 + 5);

const codeSubtract = (45 - 5);

const codeMultiply = (3 * 13);
//bring variables into alert ($)
alert(`The combination is ${codeAdd} - ${codeSubtract} - ${codeMultiply}`);